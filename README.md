# FinTrack Pro Frontend

A React + Vite + TypeScript frontend for FinTrack Pro, styled with Material UI and Tailwind CSS.  
Provides a responsive dashboard, authentication, and bill gallery integration with the backend API.

---

## 🚀 Features
- React + Vite + TypeScript
- Material UI + Tailwind CSS for UI
- Redux Toolkit for global state management
- JWT authentication with auto‑logout on expiry
- Bill Gallery (upload, view, delete bills)
- Responsive dashboard layout

---

## 📦 Installation
```bash
# Clone the repo
git clone https://github.com/AsanI2003/fin-track-project-frontend.git
cd fin-track-project-frontend

# Install dependencies
npm install


🛠️ Development
# Start dev server
npm run dev

Frontend runs on http://localhost:5173 (localhost in Bing) by default.
